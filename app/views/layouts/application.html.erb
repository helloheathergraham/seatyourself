<!DOCTYPE html>
<html>
<head>
  <title>Seat Yourself</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= stylesheet_link_tag "main" %>
  <link href="http://fonts.googleapis.com/css?family=Fjalla+One|Cantarell:400,400italic,700italic,700" rel="stylesheet" type="text/css"/>
  <link href='http://fonts.googleapis.com/css?family=Satisfy' rel='stylesheet' type='text/css'>
  <link href='http://fonts.googleapis.com/css?family=OpenSans' rel='stylesheet' type='text/css'>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
    
     <header>
       <div class="frame">
       	<div class="col2">
       		<h1><%= link_to "Seat Yourself", restaurants_path %></h1>
       	</div>
         <div class="col2">
           <div class="main-nav">
                 <% if current_user %>
        Signed in as <%= link_to current_user.name, users_path %>. <%= link_to "Log out", session_path("current"), :method => :delete %>
      <% else %>
        <%= link_to "Log in", new_session_path %> or <%= link_to "Sign up", new_user_path %>
      <% end %>
          </div>
         </div>
       </div>
     </header>
            <% if flash[:notice] %>
              <p id="notice"><%= flash[:notice] %></p>
            <% end %>
            <% if flash[:alert] %>
              <p id="notice"><%= flash[:alert] %></p>
             <% end %>
  <section>
       <div class="frame">
           <div class="col1 main-content">

    				<%= yield %>
           </div>
       </div>
   </section>
</body>
</html>
